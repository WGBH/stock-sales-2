<div class="container top">

  <div class="row">
    <div class="col-md-5">
      <p class="head">WGBH Collection</p>
      <h1 class="col-md-offset-2"><img src="<%= @collection.thumbnail_url %>"/></h1>
    </div>
    <div class="col-md-10 pull-right blurb">
      <%= @collection.long_html.html_safe %>
    </div>
  </div>

</div>

<br/>

<div class="container">

  <div class="row">  
    
    <div class="col-md-11 pull-left" id="home-left">
      <% @collection.grid_items.each do |item| %>
        <div class="col-md-5" style="float: left;" <%# Float for two columns when narrow. %> >
          <a href="<%= item.url %>" class="thumb">
            <%# TODO: Get rid of BRs and do it the right way. %>
            <img src="<%= item.thumbnail_url %>"><br/>
            <%= item.title %><br/>
            <small><%= item.subtitle %></small><br/><br/>
          </a>
        </div>
      <% end %>
    </div>
    
    <div class="col-md-4" id="home-right">
      <p>TODO: Is this right? Same search options as on the homepage?
        Or maybe we don't want to steer folks in that direction?</p>
 
      <%= render partial: 'shared/sidebar' %>

    </div>
    
  </div>
</div>
